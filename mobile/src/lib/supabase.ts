/**
 * lib/supabase.ts — STUB (Phase 3 menggunakan mock auth)
 *
 * File ini sengaja dikosongkan agar tidak gagal di-import.
 * Saat integrasi Supabase betulan, isi ulang dengan createClient(...)
 * dan ganti `lib/authService.ts` agar memanggil supabase.auth.*.
 */
export const supabase = null;
